Options -Indexes

<IfModule mod_authz_core.c>
    <FilesMatch "^(?:\.|.*\.sql)$">
        Require all denied
    </FilesMatch>
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^secure/ - [F,L]
</IfModule>
